petal::route_file!(spec:petal::static_read_spec(),read:|_:&petal::Ctx|petal::read_json_value(&"Pump.fun Petal: read coin and status information, then buy or sell an existing coin from the Bloom account you already selected. Every trade is one transaction the owner approves in Bloom; there is no session key, no separate wallet, and no spending budget. An empty token account can be closed, returning its rent to the same account."));
